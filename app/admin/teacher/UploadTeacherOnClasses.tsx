import { Button } from "antd"


const UploadTeacherOnClasses = ({
    refetch
}: { refetch: () => void }) => {

    return (
        <Button>
            导入数据
        </Button>
    )
}
export default UploadTeacherOnClasses